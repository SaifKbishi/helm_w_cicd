apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm_w_cicd.fullname" . }}-test-connection"
  labels:
    {{- include "helm_w_cicd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm_w_cicd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
