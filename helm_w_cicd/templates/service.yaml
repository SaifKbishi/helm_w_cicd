apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_w_cicd.fullname" . }}
  labels:
    {{- include "helm_w_cicd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm_w_cicd.selectorLabels" . | nindent 4 }}
